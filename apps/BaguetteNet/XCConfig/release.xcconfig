#include "shared.xcconfig"

// Per-target Release settings in Project.swift select the Developer ID
// identity and matching Mac Team Direct provisioning profile. They must be
// target-specific because the app and system extension use different profiles.
